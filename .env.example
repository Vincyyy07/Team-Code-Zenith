# Server Configuration
PORT=4000
NODE_ENV=production

# Security - CHANGE THIS IN PRODUCTION!
JWT_SECRET=your-super-secure-random-jwt-secret-key-change-this

# Database Configuration (PostgreSQL)
# Format: postgres://username:password@host:port/database
DATABASE_URL=postgres://postgres:password@localhost:5432/interview_ai

# For cloud deployment (Heroku, Railway, Render, etc):
# DATABASE_URL=postgres://user:pass@host.region.provider.com:5432/dbname

# Frontend URL (for CORS if needed)
VITE_API_URL=http://localhost:4000
